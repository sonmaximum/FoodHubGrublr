{{#each menu.menuSections as |menuSection|}}
  {{my-show-menu-section menuSection=menuSection
                      createMenuItem='createMenuItem'
                      deleteMenuItem='deleteMenuItem'
                      deleteSection='deleteSection'
                    }}
{{/each}}
{{#if menu.editable}}
  {{new-menu-section createMenuSection='createMenuSection'}}
{{/if}}